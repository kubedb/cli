apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  labels:
    app: pgadmin
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: http
    name: http
  type: LoadBalancer
  selector:
    app: pgadmin
